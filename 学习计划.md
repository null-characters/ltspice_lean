# Buck 电路进阶学习计划

> 基于现有进度（开环 → 偏差分析 → 自举 → DCM/CCM → 滞环闭环 → 数字移植）
> 面向嵌入式工程师的 SPICE→固件全链路深化路线

---

## 路线总览

```
阶段1 控制律深化 ──→ 阶段2 先进控制 ──→ 阶段3 工程化 ──→ 阶段4 综合
(PI+稳定性)      (峰值电流)         (保护+效率)     (数字电源原型)
```

| 优先级 | 实验 | 核心收获 | 嵌入式映射 |
|--------|------|----------|-----------|
| P0 | 1.1 滞环→PI | P/I 权重直觉 | PID 整定 |
| P0 | 1.2 环路稳定性 | 相位裕度 | 固件 PID 参数依据 |
| P0 | 1.3 参数敏感性 | 设计裕量 | 参数容差设计 |
| P1 | 2.1 峰值电流控制 | 电流环结构 | 数字电流环固件 |
| P1 | 3.1 保护功能 | 状态机逻辑 | 固件保护策略 |
| P1 | 3.2 效率损耗 | 损耗分布 | 器件选型 |
| P2 | 4.1 综合原型 | 系统整合 | 完整固件框架 |
| P2 | 4.2 数字移植v2 | 数字化完整链路 | ADC/定点/延迟 |

---

## 阶段 1：控制律深化（P0）

### 实验 1.1 滞环 → 真 PI

**目标**：给 Gint 加比例项，理解 P/I 各自作用。

**做法**：
```spice
* 在 Gint 表达式加比例项 Kp×(Vout-Vref)
Gint 0 dctrl VALUE={ 原积分项 + 1m*(V(out)-V(ref)) }
```
- 固定 I，扫描 Kp（.step Kp 0.5m 5m 0.5m）
- 观察：阶跃响应（Vref 突变）的超调、调节时间、稳态误差

**验证标准**：
| 观察项 | 纯 I | 加 P |
|--------|------|------|
| 响应速度 | 慢（200V/s）| 快（随 Kp 增大）|
| 超调 | 小 | 增大 |
| 稳态精度 | 无静差 | 无静差（仍有 I 项）|

**嵌入式映射**：PID 中 Kp 决定"即时反应"，Ki 决定"消除静差"——参数整定的物理直觉。

### 实验 1.2 环路稳定性（.ac 分析）

**目标**：测控制环路增益，理解相位裕度。

**做法**：
- 在误差节点断开环路，注入 .ac 小信号源（1V 交流）
- 测环路增益幅频/相频 → 波特图
- 改变 L/C/Kp，观察**增益穿越频率**和**相位裕度**变化
- 找到不稳定边界（相位裕度 < 45° 时振荡）

**验证标准**：
- 稳定判据：穿越频率处相位裕度 > 45°
- 观察：L 增大 → 穿越频率降低；Kp 增大 → 裕度减小

**嵌入式映射**：数字电源 PID 参数必须有相位裕度依据，不能盲调——这是固件稳定性设计的理论根基。

### 实验 1.3 参数敏感性扫描（.step）

**目标**：扫描 L/C/增益，建立参数-性能映射。

**做法**：
```
.step param L 50u 200u 50u
```
- 记录：纹波、穿越频率、相位裕度随 L/C 变化
- 找出稳定边界和最优区间

**验证标准**：输出"参数 × 指标"表格，标出推荐工作区。

**嵌入式映射**：固件参数容差设计——知道"电容老化 20% 后系统还稳不稳定"。

---

## 阶段 2：先进控制（P1）

### 实验 2.1 峰值电流模式控制

**目标**：实现业界最常用的电流内环控制。

**做法**：
- 结构：电压外环（PI）+ 电流内环（比较器 + RS 触发器）
- 电流采样：用 0.1Ω 采样电阻或电流互感器（网表里加 Rsense）
- **斜坡补偿**：比较器加斜坡信号，避免占空比 >50% 时次谐波振荡
- 对比电压模式：负载突变响应、限流能力

**验证标准**：
| 指标 | 电压模式（现）| 峰值电流模式 |
|------|--------------|--------------|
| 负载突变响应 | 慢 | 快（电流环先动）|
| 过流保护 | 需另加 | 天然限流 |
| 稳定性 | 二阶 | 一阶（更容易补偿）|

**嵌入式映射**：数字电流环 = ADC 高速采样 + 定时器 PWM 比较（硬件比较器），是数字电源标准结构。

---

## 阶段 3：工程化（P1）

### 实验 3.1 保护功能

**目标**：把教学电路变成可产品化原型。

**做法**：
- **过流保护（OCP）**：检测 IL > 阈值 → 强制关断 PWM（打嗝模式 hiccup）
- **过压保护（OVP）**：Vout > 阈值 → 关断 M1
- **软启动**：完善 Vref 斜坡（现有雏形），防上电浪涌
- 用 B 源 + RS 触发器实现保护状态锁存

**验证标准**：注入过流/过压故障，观察保护动作与恢复（打嗝重启）。

**嵌入式映射**：固件保护状态机——`正常运行 → 故障检测 → 保护动作 → 恢复判定`，与硬件电路一一对应。

### 实验 3.2 效率与损耗分析

**目标**：量化各部分损耗，指导选型。

**做法**：
- 分别积分 MOSFET 导通损耗、开关损耗、二极管损耗、电感铜损
- 对比 DCM vs CCM 效率
- 用同步整流（MOSFET 替代二极管）提升效率

**验证标准**：输出损耗分布饼图 + 效率曲线（负载 vs 效率）。

**嵌入式映射**：器件选型（Rds(on)、Q_g、Vf）与热设计依据。

---

## 阶段 4：综合（P2）

### 实验 4.1 完整数字电源原型（SPICE）

**目标**：合并所有模块。

**做法**：PI 控制 + 峰值电流 + 保护 + 软启动 + 同步整流 → 一个完整网表。

**验证标准**：全功能仿真通过（启动、稳压、负载突变、故障保护）。

### 实验 4.2 数字移植 v2（MATLAB/C）

**目标**：完整数字化链路。

**做法**（基于现有移植工程 buck_digital_port/）：
- 移植 PI、保护、软启动到 MATLAB/C
- 加入**硬件数字化**：ADC 量化（12-bit）、定点数（Q15）、采样延迟
- 对比理想 vs 数字化精度损失

**验证标准**：量化后控制精度损失 < 可接受范围，保护逻辑时序正确。

**嵌入式映射**：完整固件框架——ADC 中断采样 → 定点 PID → 保护状态机 → 定时器 PWM。

---

## 时间与顺序建议

```
第 1-2 周：实验 1.1 → 1.2（核心，先建立稳定性直觉）
第 3 周：  实验 1.3（参数敏感性）
第 4-5 周：实验 2.1（峰值电流，最难最有价值）
第 6 周：  实验 3.1 + 3.2（保护 + 效率）
第 7-8 周：实验 4.1 + 4.2（综合 + 数字化完整链路）
```

## 每个实验的固定产出

1. 网表文件（.cir）+ 仿真数据
2. 波形/图表（波特图、响应曲线、损耗分布）
3. 学习笔记（追加到 buck学习记录.md）
4. 嵌入式映射小结（"这个模拟行为对应固件里的 XXX"）

---

*计划制定日期：2026-08-11*
