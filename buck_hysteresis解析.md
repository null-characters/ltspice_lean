# buck_hysteresis.cir 网表解析

> 滞环占空比闭环控制 Buck 电路 —— 逐行/逐模块解析

---

## 1. 文件总览

```
文件名：buck_hysteresis.cir
电路：Buck 降压 + 自举驱动 + 滞环闭环控制（非 PI）
特性：50kHz 固定频率 PWM，Vref 可编程（10ms 处 6V→8V）
```

### 信号链

```
Vref(目标) ──┐
             ├→ Gint 滞环积分 → dctrl(占空比指令)
Vout(反馈) ──┘         │
                        ↓
锯齿波 tri ──→ Bpwm 比较 ──→ PWM ──→ B1 驱动 ──→ M1 开关
                                                      ↓
                                        L1/D1/C1 功率级 → Vout
```

---

## 2. SPICE 语法基础（读网表前必看）

### 2.1 首字母即元件类型

SPICE 网表每一行的**第一个字母决定元件类型**，这是整个语法的核心约定：

| 首字母 | 元件类型 | 示例 |
|--------|----------|------|
| V | 电压源 | `Vin in 0 12` |
| I | 电流源 | `I1 a 0 1m` |
| R | 电阻 | `R1 out 0 10` |
| C | 电容 | `C1 out 0 100u` |
| L | 电感 | `L1 sw out 10u` |
| D | 二极管 | `D1 0 sw D` |
| M | MOS 管 | `M1 in gate sw 0 NMOS` |
| B | 行为源（V=输出电压 / I=输出电流）| `Bpwm pwm 0 V=if(...)` |
| **G** | **受控电流源**（VALUE=输出电流）| `Gint 0 dctrl VALUE={...}` |
| E | 受控电压源（VALUE=输出电压）| `E1 out 0 VALUE={...}` |

> 实例名首字母必须是类型字母（如 `Gint`、`G1`、`Gcurrent` 都行，首字母必须 G 才是电流源）；名字本身只是标签。

### 2.2 元件字段位置的含义

以 `Gint 0 dctrl VALUE={...}` 为例：

```
Gint     0        dctrl       VALUE={...}
①        ②        ③           ④
实例名    正节点    负节点       行为表达式
```

| 字段 | 含义 |
|------|------|
| ① 实例名 | 首字母 G 决定类型为电流源 |
| ② 正节点 `0` | 电流从该节点"流出"源 |
| ③ 负节点 `dctrl` | 电流"流入"该节点 |
| ④ `VALUE={...}` | 输出电流 = 表达式结果（单位自动为 A）|

**方向约定**：`Gint 0 dctrl` = 正电流从 0 流向 dctrl → 给 dctrl 充电（正电流充电，负电流放电）。

### 2.3 电流源 vs 变量

- **变量**：只存一个数，不参与电路
- **电流源（G）**：**真实电路元件**，强制在 ②③ 节点间流过表达式给定的电流

仿真器每步计算 `I = 0.2m*(σ₁-σ₂)` → 注入节点 → 影响 dctrl 电压 → 形成闭环。**它实实在在地"做事"，不是存储数据。**

### 2.4 B 源 vs G 源（行为源写法对照）

| 类型 | 首字母 | 输出 | 写法 |
|------|--------|------|------|
| 行为电压源 | B | 电压 | `Bpwm pwm 0 V=if(V(tri)<V(dctrl),12,0)` |
| 行为电流源 | G | 电流 | `Gint 0 dctrl VALUE={0.2m*(σ₁-σ₂)}` |

> 类比：G 元件 = "可编程电流管"——管身写着流过多大电流（表达式），两端接在 ②③ 节点上。

---

## 3. 文件头（1-4 行）—— 注释

```spice
*Buck Converter - Hysteresis Duty-Cycle Control (Closed Loop)
*50kHz PWM (100kHz long sims >14ms hang in batch mode), duty adjusted by hysteresis:
*  Vout < Vref-0.1 -> duty up ; Vout > Vref+0.1 -> duty down ; else hold
*Output regulated to Vref +/- 0.1V. Vref steps 6V->8V at 10ms.
```

- `*` 开头 = 注释行，LTspice 忽略
- 记录：电路类型、**50kHz 选择原因**（100kHz 长仿真 >14ms 会卡死）、控制逻辑、Vref 突变设计

---

## 4. 输入电源（第 6 行）

```spice
Vin in 0 12
```

| 语法 | 含义 |
|------|------|
| `Vin` | 电源实例名 |
| `in` / `0` | 正端节点 / 负端节点（0 = 地）|
| `12` | 直流电压值 |

→ 12V 直流输入。

---

## 5. 滞环控制器（8-28 行）—— 核心模块

### 4.1 锯齿波载波（9-11 行）

```spice
Vtri tri 0 PULSE(0 12 0 19.6u 0.2u 0.2u 20u)
```

**PULSE 参数格式：**
```
PULSE(V1 V2 Tdelay Trise Tfall Ton Tperiod)
PULSE(0  12 0      19.6u 0.2u  0.2u 20u)
```

| 参数 | 值 | 含义 |
|------|-----|------|
| V1/V2 | 0/12V | 低电平/高电平 |
| Tdelay | 0 | 延迟 |
| Trise | 19.6µs | 0→12V 线性上升时间 |
| Tfall | 0.2µs | 12V→0 下降时间 |
| Ton | 0.2µs | 高电平保持 |
| Tperiod | 20µs | 周期 = 50kHz |

**注意**：Trise+Ton+Tfall = 19.6+0.2+0.2 = **20µs = 周期**，否则波形溢出异常。
→ 形成 0~12V 锯齿波，斜率 12V/19.6µs，作为 PWM 比较载波。

### 4.1.1 设计知识：为什么用"梯形"近似"锯齿"？

**波形本质**：98% 时间线性上升 + 1% 平顶 + 1% 快降。
- 从**控制功能**上 ≡ 理想锯齿波：比较器只看电压何时达到 V(dctrl)，上升斜坡线性即可保证 PWM 线性调制关系

**严格术语**：PULSE 源天生是**梯形波**——用"长上升沿 + 短下降沿"拼凑出锯齿效果（对称三角波是 50% 升 + 50% 降）

**仿真收敛优化（数值整形技巧）**：
- 理想锯齿波波峰处 12V→0V 瞬间跳变 = 不可导突变 → SPICE 反复迭代，易报 `Time Step Too Small`
- 加 0.2µs 平顶/快降 → 给出明确斜率（dv/dt = 60V/µs），连续可导 → 仿真顺畅
- **代价可忽略**：最大占空比理论极限 99%（非 100%），1% 牺牲换几百倍仿真加速

**一句话**：物理上是锯齿波，代码里是梯形波参数，目的是仿真不死机。

### 4.2 参考电压（12-13 行）

```spice
Bref ref 0 V=6+2*limit((time-10m)/0.1m,0,1)
```

- `Bref` = 行为电压源（输出由表达式计算）
- `time` = 当前仿真时间
- `(time-10m)/0.1m`：10ms 前为负，之后线性增大
- `limit(x,0,1)`：钳位到 0~1

| 时间段 | 表达式值 | Vref |
|--------|----------|------|
| 0~10ms | 0 | **6V** |
| 10~10.1ms | 0→1 | 6V→**8V** 斜坡 |
| >10.1ms | 1 | **8V** |

→ 用 `limit()` 生成平滑斜坡，避免瞬间跳变导致数值崩溃。

### 4.2.1 设计知识：为什么用斜坡而不是 IF 跳变？

**为什么不写 `IF(time<10m, 6, 8)`（阶跃）？**
- IF 在 10ms 瞬间 6V→8V = 不可导突变（dv/dt→∞）
- SPICE 牛顿-拉夫逊迭代遇"竖直墙" → 步长无限缩小 → 报 `Time step too small` 卡死

**斜坡写法（20,000 V/s）**：连续可导，仿真器大步流畅通过。

**工程意义（软启动）**：
- 0~10ms：先在 6V 稳态站稳，避免上电瞬间电感电流计算发散
- 10~10.1ms：缓升 ≈ 模拟真实硬件参考电压随电容缓慢建立，避免浪涌电流（Inrush）
- >10.1ms：锁定 8V

**limit() 的隐藏特性（硬钳位）**：
- `time<10ms` → `(time-10m)/0.1m` 为负 → limit 返回 0
- `time>10.1ms` → 为正 → limit 返回 1
- 无论 time 多大，输出永远 ≤1 → **Vref 恒在 6V~8V，8V 是天花板、6V 是地板**，不会漂移

**参数修改速查**：
| 想改 | 改哪里 |
|------|--------|
| 爬坡起点 | `10m` |
| 爬坡陡峭度 | 分母 `0.1m`（越小越陡）|
| 目标电压 | `6+2*...` → `Vstart+(Vend-Vstart)*...` |

**一句话**："前 10ms 稳定 6V，0.1ms 丝滑过渡到 8V 并稳住"——为了不让仿真器因电压突变而"抽风"。

### 4.3 滞环积分器（14-22 行）—— 核心中的核心

```spice
Gint 0 dctrl VALUE={0.2m*(1/(1+exp((V(out)-V(ref)+0.1)*20)) - 1/(1+exp((V(ref)+0.1-V(out))*20)))}
Cdctrl dctrl 0 1u ic=6
Rdc dctrl 0 100meg
```

**`Gint`** = 行为电流源，电流由 sigmoid 表达式决定：
- `1/(1+exp(z))`：sigmoid 函数（z 很负→1，z 很正→0，平滑过渡）
- 第一项 `1/(1+exp((Vout-Vref+0.1)*20))`：
  - `Vout < Vref-0.1` → 接近 **+0.2mA**（充电）
- 第二项 `1/(1+exp((Vref+0.1-Vout)*20))`：
  - `Vout > Vref+0.1` → 接近 **-0.2mA**（放电）
- 窗口内（Vref±0.1）→ 两项抵消 ≈ **0**（保持）

| Vout 状态 | Gint 电流 | dctrl 方向 |
|-----------|-----------|------------|
| 低于下阈值 | +0.2mA | 充电，占空比↑ |
| 窗口内 | ≈0 | 保持（电容记忆）|
| 高于上阈值 | -0.2mA | 放电，占空比↓ |

**`Cdctrl`**：积分电容，`Gint` 电流充放电 → `dctrl` 电压 = 占空比指令。
- `ic=6` = 初始条件 6V = 50% 占空比
- **电容的记忆 = 滞环死区**

**`Rdc`**：100MΩ 泄漏电阻，给 dctrl 直流参考点（防 .op 悬空）。

### 4.3.1 设计知识：limit vs sigmoid —— 为什么各取所长

**场景：** Bref（Vref 斜坡）用 `limit()` 线性斜坡；Gint（滞环积分）用 sigmoid 软过渡。为什么同一份代码里两种风格？

| 对比项 | limit 线性斜坡（Bref 用） | sigmoid 软过渡（Gint 用） |
|--------|--------------------------|---------------------------|
| 表达式 | `6+2*limit((time-10m)/0.1m,0,1)` | `0.2m*(σ₁ - σ₂)` |
| 过渡特性 | **硬转折**：值连续，但斜率在拐点突变 | **全程光滑**：C∞ 连续可导 |
| 到达目标 | **精确**（硬钳位终点）| **渐近逼近**（永不真正相等）|
| 工作方式 | 10ms 处**只切换一次**，之后不再回来 | Vout **每周期反复穿越**窗口边界 |
| 对硬转折容忍度 | 高（一次通过，数值影响小）| 低（反复触发会啃"尖角"）|

**为什么 Bref 用 limit：**
- 一次性动作，limit 的拐点一次通过，仿真无压力
- 硬钳位保证 Vref **精确钉在 8V**（sigmoid 渐近反而做不到）

**为什么 Gint 用 sigmoid：**
- Gint 的物理场景：Vout 带纹波在窗口边界**来回振荡**，每周期经历"充电→停→放电"方向切换
- limit 硬死区 → 电流瞬间 ±0.2mA↔0 硬跳变 → Newton-Raphson 反复啃尖角 → 步长被迫缩小 → 收敛压力大
- sigmoid 软过渡 → 电流平滑滑向 0，连续可导 → 迭代顺畅

**一句话：** 一次性动作（Bref）用 limit 的硬钳位换"精确+简单"；周期性反复动作（Gint）用 sigmoid 的软过渡换"收敛"。两者各取所长，不是风格偏好。

### 4.3.2 深度解析：Gint = 带死区（滞环带宽）的限幅积分控制器

> 定性：Gint 是**带滞环死区的限幅积分控制器**（窗口外恒速调节、窗口内锁死保持）。
> 注意：它不是 PI——**只有积分项、无比例项**；"积分分离式 PI"的解读是拔高，不准确。

**① 参数 `20` 与 `0.1` 的窗口设计**
- `0.1` = 窗口半宽（Vref±0.1V 静默区）；`20` = 陡峭系数
- 边界验证：`Vout = Vref-0.1` → 第一项指数 `(-0.1+0.1)*20 = 0` → sigmoid = 0.5，电流为峰值一半 → **平滑过渡非硬切**
- 修正：实际过渡带约 ±0.15V 才进入饱和（略宽于窗口）

**② 积分速率 = 物理响应时间（C=1µF）**
```
dv/dt = I/C = 0.0002A / 0.000001F = 200 V/s
dctrl 从 6V(50%) 充到 12V(100%) 需 30ms = 1500 个开关周期(50kHz)
```
→ 占空比缓慢平滑跟随误差，防止过冲（= 软启动特性）

**③ 双向限幅差分电流源 —— ±0.2mA 是饱和，不是比例项**
- Vout 远低 → +0.2mA（充电↑）；远高 → -0.2mA（放电↓）
- 关键点：**sigmoid 的 0.5 中心在窗口边界（Vref±0.1），不在 Vref 中心**
  - `Vout=Vref` 时指数参数 = `(0+0.1)*20 = 2` → sigmoid = 1/(1+e²) = 0.119，两项相减 = 0
  - 中心处斜率 ≈ 0.4mA/V（低增益线性区）
- **为什么不能看成比例项**（比例要求电流随误差线性增长）：

  | 误差 e | 理想比例电流 | 实际 Gint 电流 |
  |--------|-------------|---------------|
  | 0.3V | 0.12mA | **0.2mA（饱和）** |
  | 1.5V | 0.6mA | **0.2mA（饱和）** |
  | 10V | 4mA | **0.2mA（饱和）** |

  误差 0.3V→10V 电流纹丝不动 = 饱和限幅，非比例。
- 唯一"伪比例区"：sigmoid 过渡带（窗口边界外 ±0.05V，斜率≈0.4mA/V），但宽度仅 ±0.05V，正常工作点不在该区
- 如何变真 PI（扩展思路）：加即时比例项 `Gint VALUE={原积分项 + 1m*(V(out)-V(ref))}`（调节更快但可能牺牲稳定性）

**④ 电容记忆 = 滞环死区（无静差调节）**
- 即使 Vout=Vref、电流=0，dctrl 保持当前值不回落 → 允许输出在窗口内自由浮动，不追求固定占空比
- 这正是"滞环"的本质：死区 + 记忆；PWM 本身继续 50kHz 开关，只是占空比停住
- **模拟↔数字对照**（积分 = 记忆 = 状态保持，三种载体本质相同）：

  | 领域 | 载体 | 本质 |
  |------|------|------|
  | 模拟硬件 | **电容**（电荷存储）| 电压 = 积分电荷 |
  | 数字控制 | **寄存器/变量**（`duty += delta`）| 每次中断累加 |
  | 控制理论 | **积分器状态 x** | x[k+1] = x[k] + u·T |

  ```c
  // 数字实现（每 20µs 中断）
  if (vout < vref-0.1)      duty += 1;   // 窗口外 +1
  else if (vout > vref+0.1) duty -= 1;   // 窗口外 -1
  // 窗口内：不执行任何增减 → duty 保持原值（寄存器天然记忆）
  ```

  > 行为级建模的钥匙：**spice 里的每个电容/积分器，都能在数字代码里找到对应的状态变量**。电容 = 物理世界的"寄存器"。

  **信号链（电压→电流→电容）：**
  ```
  V(out) / V(ref) ──→ Gint 计算 ──→ 电流 I（±0.2mA）
                                        │ 给 Cdctrl 充电/放电
                                        ↓
                        dctrl 电压（= 占空比指令）按 200V/s 变化
  ```
  即"电压输入 → 电流输出"（VCCS）→ 电流注入电容 = 物理积分。

  **精细区别：是"累加"不是"赋值"**

  | 操作 | 数字 | 物理（电容）|
  |------|------|-------------|
  | 赋值 | `duty = 60`（瞬间覆盖）| 电压**不能突变**，只能按 200V/s 爬过去 |
  | 累加 | `duty += 1`（保留旧值+增量）| 电压从当前值**连续变化** ✓ 更接近 |

  电容电压 = 6V + (1/C)·∫I·dt → **天然记住历史**（当前电压 = 之前所有电流的积分）。
  每周期（20µs）+0.2mA → 电压 +0.004V = 数字里的 `duty += 1`。

**⑤ Rdc 100MΩ 的救命稻草作用**
- .op 时电容开路，dctrl 无直流路径 → 会报 `Node dctrl is floating`
- 100MΩ 提供地参考；时间常数 1µF×100MΩ = 100s，远大于 30ms 调节，不影响工作
- 修正：**ic=6 来自 `Cdctrl ic=6` 初始条件（配合 `.tran uic`），不是 Rdc 的作用**

**控制类型定性（准确归类）：**
```
死区（±0.1V）+ 饱和非线性（±0.2mA 恒流）+ 积分器（电容）
= 带死区的饱和积分控制器（更接近 Bang-Bang + 积分 / 滑模简化形式）
```
- PI 的 P 项提供"即时响应"；当前无即时出力 → 纯 I 行为 → **P 项不存在，不能叫 PI**
- 本意视角（滞环）：死区带宽 = 滞环窗口，**以滞环为主的控制**，积分器只是实现占空比调节的手段

**动态画面：**
> Vout 偏低 → +0.2mA 涓流给 1µF 充电 → dctrl 以 200V/s 爬升 → 占空比加大 → Vout 回升 → 进入窗口后涓流归零，dctrl 定格。

**定性结论：** 滞环（死区）逻辑 + 积分器记忆 + 电流限幅，天生防抖、无静差。

### 4.3.3 延伸：SPICE 模型 ↔ 真实硬件 ↔ 数字控制

**1. 真实硬件映射表（SPICE 行为源 = 真实芯片内部电路）：**

| SPICE 行为源 | 真实硬件元件 |
|--------------|--------------|
| Gint（电压→电流判断）| **误差放大器（运放）** |
| ±0.2mA 电流 | 运放输出经限流电阻 |
| Cdctrl 电容 | **补偿电容**（芯片引脚上的 C）|
| 滞环窗口 | 运放 + 反馈电阻的阈值 |
| Bpwm 比较器 | PWM 比较器 |
| 锯齿波载波 | 内部振荡器（斜坡发生器）|

**2. 真实 Buck 控制芯片内部结构（UC3842/LM2596 等）：**
```
Vout 分压 ──→ 误差放大器 ──→ 补偿电容(积分) ──→ 比较器 ──→ PWM
                 ↑                                  ↑
              Vref(基准)                        锯齿波载波
```
→ **模拟实现不是仿真特有的技巧，而是真实硬件的做法**。

**3. 数字逻辑的物理底层 = 模拟电路：**
- 数字芯片（MCU/FPGA）的寄存器、加法器、比较器，物理上都是**晶体管组成的模拟电路**
- "数字"只是对模拟世界的**量化抽象**（电压高于阈值=1，低于=0）

**4. 模拟 vs 数字控制取舍：**

| | 模拟控制 | 数字控制（MCU）|
|--|----------|----------------|
| 实现 | 运放+电容+比较器 | 固件（ADC+算法+PWM）|
| 优点 | 响应快(ns)、无量化误差、成本低 | 灵活、可通信、支持复杂算法 |
| 缺点 | 改参数需改元件 | 采样延迟、量化误差 |

**结论：** SPICE 模型、模拟硬件、数字代码描述**同一个控制律**，只是载体不同——模拟用电荷/电压，数字用变量，底层都是晶体管电路。

### 4.3.4 代码级对照：SPICE 元件 → C 语言

**1. 可直接转 C 的（控制逻辑类）：**

| SPICE 元件 | C 语言等价 |
|------------|-----------|
| 滞环积分器 Gint+C | `if (vout < vref-0.1) duty += 1; else if (vout > vref+0.1) duty -= 1;` |
| 二极管限幅（Dclamp）| `if (duty < 0) duty = 0; if (duty > 100) duty = 100;` |
| Bpwm 比较器 | `pwm = (tri < duty_cmd) ? 1 : 0;` |
| Bref 斜坡 | `vref = 6 + 2*clamp((t-10ms)/0.1ms, 0, 1);` |
| sigmoid 平滑 | `sigma = 1/(1+exp(...))`（C 里 `exp()` 照样写）|

**2. 只能"等效实现"的（物理器件类）：**

| SPICE 元件 | C 语言怎么做 |
|------------|--------------|
| **电容积分** | 无电容 → **离散积分**：`duty += delta`（每周期累加）|
| **电感** | 无电感 → 状态方程离散化：`iL += (vL/L)*T` |
| **MOS 开关/二极管** | 无物理器件 → **if 判断导通状态**模拟 |

> 数字控制器里没有真实电容电感，用**离散时间方程（差分方程）**在数学上复现动态——这就是状态空间/数字滤波器思路。

**3. 完整对照：滞环 Buck 转 C（伪代码）：**
```c
// 每 20µs 中断一次（=50kHz）
float duty = 50;                      // Cdctrl 初值 ic=6 → 50%

// Gint 滞环积分器
if (vout < vref - 0.1)      duty += 0.2;   // 窗口外 +（充电）
else if (vout > vref + 0.1) duty -= 0.2;   // 窗口外 -（放电）
// 窗口内：不动（duty 保持 = 电容记忆）

// 二极管限幅
if (duty < 0)   duty = 0;
if (duty > 100) duty = 100;

// Bpwm 比较器
pwm = (tri < duty) ? ON : OFF;
```

**结论：**
- **控制逻辑**（比较/限幅/滞环/斜坡）→ 几乎 1:1 转 C
- **物理动态**（电容/电感）→ 换成离散化方程（积分→累加、微分→差分）
- 本质：SPICE 用物理元件实现数学运算，C 用变量语句实现同一套数学——**一个用电子，一个用指令，算的是同一套数**

### 4.4 dctrl 钳位（23-26 行）

```spice
Dclamp1 0 dctrl DCLAMP
Dclamp2 dctrl vcc DCLAMP
```

两个二极管把 dctrl 钳位在 **0~12V**：
- `Dclamp1`：dctrl<0 导通 → 拉回 0
- `Dclamp2`：dctrl>12V(vcc) 导通 → 拉回 12V
→ 防止积分漂移导致占空比饱和失控。

### 4.5 PWM 比较器（27-28 行）

```spice
Bpwm pwm 0 V=if(V(tri)<V(dctrl), 12, 0)
```

锯齿波 tri 低于占空比指令 dctrl → 输出 12V（导通），否则 0V（关断）。

| dctrl | 占空比 |
|-------|--------|
| 6V | 50% |
| 8V | ~67% |
| 12V | 100% |

---

## 6. 自举电路（30-37 行）

```spice
Vcc vcc 0 12        ; 12V 辅助电源
Db vcc boot D       ; 自举二极管：Vcc→boot
Cb boot sw 100n     ; 自举电容：boot↔sw（下端接 sw）
Rboot boot 0 1meg   ; 直流路径（防 .op 悬空）
```

**原理：**
1. M1 关断（D1 续流，sw≈-0.7V）→ Cb 通过 Db 充电至 ~11.3V
2. M1 导通（sw 上升）→ Cb 下端被抬高，`boot = sw + 11.3V`
3. 高边 NMOS 的 **Vgs 恒定 ~11.5V**，不随 sw 变化

`Rboot`：电容在 .op 时开路会导致 boot 悬空 → 加 100MΩ 直流路径。

---

## 7. 栅极驱动器（39-48 行）

```spice
B1 drv 0 V=V(sw)+if(V(pwm)>6, 11.5, 0)
Rg drv gate 10       ; 驱动输出阻抗
Cg gate sw 1n        ; 栅极电容
Cgnd gate 0 1n       ; 收敛辅助
```

| 元件 | 作用 |
|------|------|
| `B1` | 模拟驱动芯片：PWM 高 → drv=sw+11.5V（Vgs=11.5V）；低 → drv=sw（Vgs=0）|
| `Rg` | 真实芯片输出阻抗（10Ω）|
| `Cg` | MOS 栅极电容（1nF）|
| `Cgnd` | gate 对地电容，**打断 sw→gate→sw 代数环**（L=100µH 时必需，否则 Singular matrix）|

---

## 8. 功率级（50-62 行）—— 标准 Buck

```spice
M1 in gate sw 0 NMOS   ; 高边 NMOS：D=in, G=gate, S=sw
D1 0 sw D              ; 续流二极管：阳极=0, 阴极=sw
Rsw sw 0 10meg         ; sw 直流路径（防悬空）
L1 sw out 100u         ; 电感 100µH
C1 out 0 100u          ; 输出电容 100µF
R1 out 0 10            ; 负载 10Ω
```

- M1：高边开关（漏极接 Vin，源极接开关节点 sw）
- D1：续流（阳极接地，阴极接 sw，导通时 sw 被钳到 -0.7V）
- L1/C1：LC 滤波；R1：负载

---

## 9. 器件模型（64-67 行）

```spice
.MODEL NMOS NMOS(VTO=2 KP=10)    ; NMOS：阈值 2V，跨导 10
.MODEL D D(IS=1e-12)             ; 普通二极管（饱和电流 1e-12）
.MODEL DCLAMP D(IS=1e-14)        ; 钳位二极管（更理想，漏电更小）
```

---

## 10. 仿真指令（69-70 行）

```spice
.tran 25ms uic
```

- 瞬态分析 **25ms**
- `uic`（Use Initial Conditions）：跳过 .op 直接瞬态，从零状态开始
  - 原因：Gint 的 0.2mA × Rdc 100MΩ = 20000V 荒谬 .op 工作点
  - 配合 `Cdctrl ic=6` 指定 dctrl 初始值

---

## 11. 测量语句（72-99 行）

```spice
.meas Vout_before AVG V(out) TRIG time=2ms TARG time=9.9ms
```

| 语法 | 含义 |
|------|------|
| `AVG V(out)` | 求 V(out) 平均值 |
| `MIN/MAX V(x)` | 求最小值/最大值 |
| `TRIG/TARG` | 只统计该时间区间 |

- `Duty_xxx AVG V(dctrl)*100/12` → **占空比百分比**（dctrl 电压 / 12V × 100）
- `Dctrl_t1~t5` → 10ms 突变后每 2ms 采样，展示**占空比爬升轨迹**
- `IL_min_after MIN I(L1)` → 判断工作模式：**>0 = CCM，≈0 = DCM**

---

## 12. 结束（101 行）

```spice
.end
```

SPICE 网表必须以 `.end` 结尾。

---

## 13. 仿真结果速查（参考）

```
duty_before: 52.7%      ← Vref=6V 稳态
duty_t1:     55.5%      ← 10ms 突变后爬升
duty_t2:     58.9%
duty_t3:     62.2%
duty_t4:     65.5%
duty_t5:     68.3%
duty_after:  68.6%      ← Vref=8V 稳态
il_min_after: 0.496A    ← >0 确认 CCM
```

---

*解析日期：2026-08-11*
