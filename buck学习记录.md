# Buck 降压电路学习记录

> 网表搭建 → 仿真验证 → 排查"输出电压 ≠ 理论值" → 自举驱动

---

## 1. Buck 电路基础

### 拓扑与原理

```
Vin(12V) ──┬── M1(NMOS) ── L1 ──┬── C1 ──┬── Vout
           │                    │        │
           └──── D1 ────────────┘        R1
                                        GND
```

- **M1 导通**：Vin → M1 → L1 → 负载，电感储能
- **M1 关断**：L1 通过 D1 续流 → 负载
- **理想公式**：`Vout = Vin × D`，50% 占空比 → 6V

### 元件表

| 元件 | 值 | 作用 |
|------|-----|------|
| Vin | 12V | 输入电源 |
| Vgate | PWM 0/12V, 5µs/10µs | 开关控制（占空比50%） |
| M1 | NMOS (VTO=2) | 高边开关管 |
| D1 | 二极管 | 续流 |
| L1 | 10µH | 储能 |
| C1 | 100µF | 滤波 |
| R1 | 10Ω | 负载 |

---

## 2. 网表要点

```spice
Vin in 0 12
Vgate gate 0 PULSE(0 12 0 10n 10n 5u 10u)
M1 in gate sw 0 NMOS
D1 0 sw D
L1 sw out 10u
C1 out 0 100u
R1 out 0 10
.MODEL NMOS NMOS(VTO=2 KP=10)
.MODEL D D(IS=1e-12)
.tran 10ms
.meas Vout_avg AVG V(out) TRIG time=5ms TARG time=10ms
.end
```

- 命令行仿真：`LTspice.exe -b -Run buck.cir`
- 输出：`buck.log`（测量结果）、`buck.raw`（波形）

---

## 3. 常见问题速查

| 问题 | 原因 | 解决 |
|------|------|------|
| 中文注释乱码 | UTF-8 vs ANSI 编码 | 注释用英文 |
| `10µ` 乱码 | µ 编码问题 | 写 `10u` |
| .meas FAIL'ed | 节点名不匹配（VOUT vs out） | 统一 FLAG 与测量节点名 |
| .asc 报 MODEL 语法错 | 模型需写为 TEXT 指令 | `TEXT ... !.MODEL ...` |

---

## 4. 核心问题：Vout ≠ Vin×D（实测 6.276V，理论 6V）

### 三大偏差来源

| 来源 | 方向 | 原因 | 判断依据 |
|------|------|------|----------|
| **DCM 断续模式** | 偏高 | 纹波(3A) >> 负载电流(0.6A)，电流降到0 | IL_min ≈ 0 |
| **高边 NMOS Vgs 不足** | 偏低 | Vgs=Vgate-Vsw 被压缩到 2.58V（阈值2V） | Vsw_max=10V≠12V |
| **二极管压降** | 偏低 | Vd≈0.7V，损失 Vd×(1-D)=0.35V | Vsw_min=-0.73V |

### DCM 伏秒平衡

```
(Vin-Vout)×D = Vout×δ   (δ < 1-D)
⇒ Vout = Vin×D/(D+δ) > Vin×D
```

### 精确公式

```
Vout = Vin×D - Vd×(1-D) - I×Ron
```

---

## 5. 自举电路（解决高边 NMOS 驱动不足）

### 原理

自举电容 Cb 在 M1 关断时（sw≈0V）通过 Db 充电至 ~12V；
M1 导通时 sw 上升，Cb 下端被抬高，boot = sw+12V，Vgs 恒定。

### 网表实现

```spice
Vcc vcc 0 12
Db vcc boot D          ; 自举二极管
Cb boot sw 100n        ; 自举电容（下端接 sw）
Rboot boot 0 1meg      ; boot 直流路径（防 .op 悬空）
* 驱动芯片模型：Vgs = 11.5V (PWM高) / 0V (PWM低)
B1 drv 0 V=V(sw)+if(V(pwm)>6, 11.5, 0)
Rg drv gate 10         ; 驱动输出阻抗
Cg gate sw 1n          ; 栅极电容
```

### 验证结果（对比）

| 测量项 | 无自举 | 有自举 | 结论 |
|--------|--------|--------|------|
| Vgs_max | 2.58V | **11.5V 恒定** | ✓ 驱动不足解决 |
| Vboot_max | - | **26.6V** | ✓ 举升正常 |
| Vsw_max | 10.0V | 14.4V | MOS 充分导通 |
| Vout_avg | 6.28V | 7.83V | 仍偏高（DCM 遗留）|

### 实现经验（仿真收敛坑）

1. **自举电容使 boot 节点悬空** → 加 `Rboot` 直流路径
2. **理想开关/受控源直接驱动 gate 导致收敛失败**（Singular matrix / Heightened Def Con）
   - 改用 B 源 + 输出电阻 Rg + 栅极电容 Cg
   - 驱动器表达式只依赖 `V(sw)`，不依赖 `V(boot)`，打破反馈环

---

## 6. DCM vs CCM 对比实验

### 背景：boot 电压异常现象

**现象：** 观察 boot 节点波形，PWM 低电平期间 boot 没有回到 12V，而是停在 ~20V。

**原因：** `V(boot) = V(sw) + V(Cb)`，DCM 模式下 M1 关断期分两段：

| 阶段 | sw 电压 | boot 电压 |
|------|---------|-----------|
| ① 续流期（D1 导通） | ≈ -0.7V | 被 Db 拉回 ~11V |
| ② **空闲期（电流=0）** | **≈ Vout** | **Vout + Vcb ≈ 20V** ✓ |

电感电流降到 0 后 D1 截止、sw 不再被钳位到地，而是等于 Vout；
Cb 已充满且无放电通路 → boot = Vout + Vcb ≈ 20V。**这是 DCM 的正常特征。**

### 实验：改电感进入 CCM

仅将 `L1 10u → 100u`（文件 `buck_ccm.cir`，其余与 `buck.cir` 完全一致）。

### 结果对比（自举驱动电路）

| 测量项 | DCM (L=10µ) | CCM (L=100µ) | 说明 |
|--------|-------------|--------------|------|
| IL_min | ≈0 | **0.40A > 0** | ✓ 确认 CCM |
| Vout_avg | 7.83V | **5.72V** | CCM 更接近理论6V |
| Vsw_max | 14.4V | 12.0V | MOS 充分导通 |
| Vsw_min | -0.74V | -0.71V | 续流压降 |
| Vgs_max | 11.5V | 11.5V | 自举正常 |
| Vboot_max | 26.6V | 24.3V | 举升正常 |
| **Vboot_min** | 11.5V | 11.5V | 见下 |

### boot 低电平的 DCM/CCM 对比

| 模式 | M1 关断期间 sw 状态 | boot 低电平 |
|------|--------------------|-------------|
| DCM | 续流期 -0.7V → **空闲期 =Vout(7.8V)** | 大部分时间 ≈ **20V** |
| CCM | 续流期全程 -0.7V（电流不断） | 全程被 Db 拉回 ≈ **11.5V** |

**结论：** boot 低电平是否回到 12V 附近，可用于快速判断工作模式。

### CCM 收敛坑：代数环 Singular matrix

**现象：** 只改 L=100µ 后仿真报 `Singular matrix: check node gate`。

**原因：** 大电感使开关切换更硬（电流不为 0），驱动表达式
`B1: drv = V(sw)+11.5` 与 `M1: sw = f(Vgs)` 形成**代数环**，
环路增益过高 → 矩阵奇异。

**解决：** 加 `Cgnd gate 0 1n`（gate 对地电容），打断瞬时代数环。
1nF 相对主电路（µH/µF 级）影响可忽略，不改变电路行为。

**经验：** 对比实验应保持两文件**仅一处功能差异**（此例只有 L1），
其余注释/结构完全一致，diff 一目了然。

---

## 7. 实验验证记录（Vout 汇总）

| # | 配置 | Vout | 说明 |
|---|------|------|------|
| ① | NMOS+二极管, Vg=12V, R=1Ω | 4.17V | Vgs不足+二极管压降 |
| ② | NMOS+二极管, Vg=20V, R=1Ω | 5.58V | 仅二极管压降 |
| ③ | 理想开关+二极管, R=1Ω | 5.635V | 仅二极管压降 |
| ④ | 同步整流(双理想开关), R=1Ω | 5.99V | ≈6V ✓ |
| ⑤ | NMOS+二极管, Vg=12V, R=10Ω | 6.276V | DCM+Vgs不足巧合抵消 |
| ⑥ | NMOS+二极管, Vg=20V, R=10Ω | 7.81V | DCM 效应暴露 |
| ⑦ | 自举驱动, R=10Ω | 7.83V | DCM 遗留问题 |
| ⑧ | 自举驱动+CCM(L=100µ), R=10Ω | 5.72V | ✓ 最接近理论值(仅剩二极管压降) |

---

## 8. 经验总结

1. **Vout = Vin×D 仅在 CCM + 理想器件下严格成立**
2. **判断工作模式看电感电流**：IL_min > 0 → CCM；≈0 → DCM（输出偏高）
3. **高边 NMOS 必须自举驱动**（或低边 NMOS / 同步整流）
4. **boot 低电平判断模式**：回到 12V 附近 → CCM；停在 ~Vout+Vcb → DCM
5. **自举仿真要点**：boot 节点加直流路径；驱动源加输出阻抗，避免理想源反馈环
6. **大电感触发代数环奇异**：驱动 B 源依赖 sw 时，加 Cgnd 打断反馈环
7. **对比实验最小差异原则**：两文件只留一处功能差异，便于直观对比
8. **µ 用 u；注释用英文；FLAG 节点名与 .meas 一致**

---

## 9. 相关文件

| 文件 | 说明 |
|------|------|
| `buck.cir` | Buck 电路网表（自举驱动，DCM，L=10µ） |
| `buck_ccm.cir` | 同 buck.cir，仅 L1=100µ（CCM）+ Cgnd 收敛辅助 |
| `buck.asc` | 原理图 |
| `buck.log` | 仿真日志 |

---

*学习日期：2026-08-11*
